const Loading = () => {
  return (
    <>
      <section className="app__loading-section">
        <h1 className="app__title">Loading</h1>
        <div className="app__loading-spinner">Loading ...</div>
      </section>
    </>
  );
};

export default Loading;
